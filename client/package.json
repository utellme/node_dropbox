{
  "name": "assign1-dropbox-client",
  "version": "1.0.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "start": "nodemon --exec babel-node -- --optional strict --stage 1 -- client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^2.9.25",
    "json-over-tcp": "^0.3.2",
    "mkdirp": "^0.5.0",
    "rimraf": "^2.3.3",
    "songbird": "^1.0.1"
  }
}
